# 実ログの読み取り検証

元ファイル: QSL(20260909-141613).txt
SHA-256: 09cb60952f98de4a53109842b58d14855cd084f568ac8ae01b2e0f227708f41d

全26071行、読取成功25943交信、要確認12行。
元ファイルへの書き込みは行っていません。

要確認は行末記号8行、12項目行4行。修正や除外を自動確定していません。

| 行番号 | 理由 |
| --- | --- |
| 471 | 行末のバックスラッシュ2文字がありません。 |
| 689 | 行末のバックスラッシュ2文字がありません。 |
| 1083 | 行末のバックスラッシュ2文字がありません。 |
| 3491 | 行末のバックスラッシュ2文字がありません。 |
| 3695 | 行末のバックスラッシュ2文字がありません。 |
| 6719 | 行末のバックスラッシュ2文字がありません。 |
| 6858 | 行末のバックスラッシュ2文字がありません。 |
| 8179 | 12項目あります（10または11項目が必要）。 |
| 17648 | 行末のバックスラッシュ2文字がありません。 |
| 18907 | 12項目あります（10または11項目が必要）。 |
| 18939 | 12項目あります（10または11項目が必要）。 |
| 25640 | 12項目あります（10または11項目が必要）。 |

年区切りの2023 /JD1を含むため、すべてをJH1HSTとして取り込むことは確定していません。仮計画上の重複は15件ですが、自局振り分け後に再判定が必要です。

基盤16テストとQt接続1テストがLinuxで成功。Windows実機は未確認。
## 検索・編集画面の追加検証（1.00 開発チェックポイント）

Python 3.12.14 / PySide6 6.8.3 / Linux offscreen、全25テスト成功。
自局完全一致と任意の移動運用包含、AND条件、分単位の両端を含む範囲、2,501件の結果、同じ交信キーの別行識別、検索後の外部変更拒否、問題行のあるファイルの変更拒否を確認。
Qt画面では不正日付、編集保存、条件変更による選択解除、削除のEnter時キャンセル、明示削除とバックアップ、未保存変更の破棄、メニューからの起動・メイン再読込・現在MY QTH維持を検証。検索画面は実際のQt描画画像でも確認。
Windows実機・EXE・年をまたぐ編集は引き続き未検証／未実装。

## バックアップ復元画面（1.00 開発チェックポイント）

Linux offscreenで全32テスト成功。追加7件は、復元と復元前への再復元、キャンセル、原本外部変更、バックアップ外部変更、削除済み・文字コード破損原本の復元、異常バックアップ拒否とフィルター変更時の選択解除、バックアップ書き込み失敗時の原本維持。Qt画面の描画も確認。
Windows実機とEXEは未検証。既存の保存基盤の制約は継続。

## PSログTXTインポート（1.00 開発チェックポイント）

全39テスト成功（Linux Qt offscreen）。年別振り分け・入力不変・重複除外／全追加・問題行除外の明示指定・運用区切りの確認・入力／保存先の外部変更・途中停止の結果記録と既定モードでの再取り込み・旧RST保持・バックアップ失敗時の原本維持・画面の条件変更／確認／連続実行防止を検証。実Qt画面も描画確認。
実ログの読み取り専用確認：25,943交信、問題12行、8年分、指定自局JH1HSTへ一律割当した仮定で追加25,928・重複15。運用区切りの自局割当は未確定。原本への取り込みは実施していない。
ファイル単位の置換であり、複数ファイルの原子的コミットではない。結果JSONのattemptingは異常終了等により未確定を意味する。前後SHA-256とバックアップパスを記録するが、自動回復は行わない。Windows実機・EXEは未検証。

## 通常エクスポート（1.00）

全46テスト成功（Linux Qt offscreen）。JST年越しからUTCへの変換、MODE/SUBMODE、FREQ非捏造、秒境界と最新N件・同時刻順、ADXの日本語とXMLエスケープ、CSV引用符と15列・幅超過停止、同名連番、外部変更、未知MODE拒否時の原本維持、画面条件変更と出力を確認。実Qt描画も確認。
CSVは一般CSVパーサーによる検査でありHAMLOG読み込み検証ではない。ADIFは出力構造検査であり外部ソフト相互運用やADX XSD検証ではない。Windows EXE未検証。マッピングの未確定点はEXPORT_MAPPING.mdを参照。

## ADIFインポート（1.00）

Linux Qt offscreenで全54テスト成功。年越し・秒・+00正規化、長さ指定と値内EOR、ヘッダーあり／なし、欠損EORと長さ異常、ADXのXMLエスケープ・APP・DTD拒否、自局不一致と明示変更、未知MODE、FREQのバンド化と元値保持、不正交信、区切り置換、保存結果の元項目保持、ADX出力からの再取り込み、変換確認と画面条件変更時の確認解除を検証。実Qt画面も確認。
全ADIF仕様の適合性試験・他ソフトとの相互運用試験は未実施。日本語ADIは未対応でADXを案内する。所在地自動補完、QSLの意味変換、MODE組合せ全検証、Windows実機・EXEは未完了。

## HAMLOG CSVインポート（1.00）

全62テスト成功（Linux Qt offscreen）。15/16列、番号付きCSV非上書き、CP932・UTF-8 BOM、CSVエスケープと複数行値、OP／Remarks／日本語QTHの保持、UTC日付繰上げ、J/U省略時の停止と指定、2桁年、異常日付・時刻・GL・周波数、未知モード、海外移動をJapanとしない動作、重複除外・結果JSON・画面からの取り込みを確認。実画面も描画確認。
合成CSVの検査であり、HAMLOG実機のエクスポート／再インポート試験は未実施。日本語所在地DBローマ字化、国外の国名自動判定、QSLフラグの意味変換は未実装。

## ブラックリスト（1.00）

全68テスト成功（Linux Qt offscreen）。登録・編集・削除・再読込、全角コール正規化、完全一致／任意のサフィックス包含、重複登録拒否、外部変更・保存障害時のファイル保持、壊れた設定を空リストで上書きしない動作、OK後の記録とRMKS維持、コールセットを経ない直接記録時の警告、照合不能時の通知と記録継続、管理画面の選択・検索・削除キャンセルを検証。Qt画面の描画も確認。
HAMLOG実機互換性は、ユーザーの利用開始後に確認し、問題があれば修正する合意。開発の進行を止めるゲートにはしない。

## 設定・全体バックアップ（1.00）

全74テスト成功（Linux Qt offscreen）。設定保存・再起動・未知の設定項目保持、記録確認OFF、設定保存失敗時の未適用、外部設定変更拒否、月次案内の同月抑制・OFF、ZIP内の原本バイト一致・保存障害・保存先制限、保持世代数と不正設定拒否を検証。Qt設定画面も描画確認。
全体バックアップはconfig/とlogbook/を含むZIP。全体ZIP専用の復元画面は未実装。Windows実機とEXEは未検証。

## ウィンドウ位置・サイズ（1.00）

全79テスト成功（Linux Qt offscreen）。欠落モニター・画面外座標・極小／不正設定の補正、負座標のモニター維持、480×300作業領域にメインを収める処理、再起動時の保存値引き継ぎ、360×240で設定画面のスクロール、最大化・最小化後の通常サイズ保持を確認。640×440で実際のQt描画も確認。
テストは模擬作業領域を含む。Windowsの実モニター抜き差し、混在DPI、ウィンドウ装飾やタスクバー配置の実機試験は未実施。

## 所在地DB・入力補助（1.00 開発チェックポイント）
全83テスト成功（Linux Qt offscreen）。1,899件の読み込み、確認済み3件、gun保持、コード不一致拒否、未確認候補の自動入力抑止・確認必須、MY QTH変更時の相手JCC保持・先頭GL保持、補助OFFの再起動保持、HAMLOGの確認済み完全一致のみの変換を検査。所在地画面も実Qt描画で確認。
全件のローマ字・所在地の最新性は未検証。履歴候補は未確認として扱う。Windows実機・EXEは未検証。

## QSL受領一括処理（1.00 開発チェックポイント）
全94テスト成功（Linux Qt offscreen）。追加11テストはQSL_BATCH.mdに記載。元ログRMKSのみの更新・旧形式保持、候補選択、CSVの更新前フラグと交信当時のMY QTH、結果保存失敗時の原本不変、バックアップ失敗、部分保存と原本保存後のCSV障害を検査した。画面描画とメニュー起動も確認。複数ファイルは一括原子更新ではなく、異常終了時の自動回復は未実装。Windows実機・EXEは未検証。

## POTA提出用ADIF（1.00 開発チェックポイント）
全101テスト成功（Linux Qt offscreen）。追加7テストの内容はPOTA_EXPORT.mdを参照。通常エクスポートから抽出処理を共通化し、既存出力のテストも成功。公園別・UTC日別の出力、原本不変、結果ファイルの非上書きと途中失敗を確認した。実際のPOTA提出・Windows実機・EXEは未検証。

## SOTA提出用CSV（1.00 開発チェックポイント）
全107テスト成功（Linux Qt offscreen）。追加6テストと出力範囲はSOTA_EXPORT.mdに記載。山頂割当の原本行への対応、UTC日ごとのCSV、必須山頂チェックと原本不変、既存機能の回帰を確認。画面2種も描画確認した。SOTAの現行アップローダー受入試験・Windows実機・EXEは未実施。

## 2026-09-11 コンテスト作業・ルール管理

全119テスト成功（Python 3.12.14 / PySide6 6.8.3 / Linux Qt offscreen）。追加12テスト：条件優先順位と重複、第二マルチの固定0・条件集計0、未知モード・地域不足の未確定、先頭ゼロ・複数数字群・バンド別積、厳密JSON検証、保存衝突・履歴・外部変更、RMKS候補、分単位範囲と自局サフィックス、102行のページ表示と戻る操作・原本不変、編集保存・不正JSON、条件GUIと未確認候補の集計抑止、対象変更キャンセル・外部変更後の再読込。

Qt画面を描画してルールのマルチ設定、対象ログ、ナンバー入力を画像確認。800ピクセル幅では表を横スクロール、交信数でウィンドウは伸びない。段階5の提出ファイル出力は未実装。公式大会ルールの採点照合・Windows実機・EXEビルドは未検証。詳細はCONTEST.md。

## 2026-09-11 コンテスト提出ファイル・Cabrilloテンプレート

全135テスト成功（Python 3.12.14 / PySide6 6.8.3 / Linux Qt offscreen）。追加16テストでJARL R1/R2のタグ差分・FD係数0・JST/UTC・不正入力、Cabrillo UTC年越し・固定列幅・実周波数必須・2.0ヘッダー・固定RSTと番号部分、zLog公式34列順・kHz周波数・UTF-8 BOM・既知/未対応モード・電力コードの明示分離を確認。非整数の出力停止、十進演算による正確な総得点、同名回避、原本/テンプレート外部変更、提出者設定の記憶対象、テンプレートGUI、段階5からのJARL/Cabrillo保存、補完画面キャンセルも確認しました。

JARLのユーザー提出済みR1.0/R2.1ファイルのタグ構造と公式書式を照合。zLogは公式リポジトリa078794a53df62d9fed756ebd4e205be372597edを読んで確認し、FreqがkHz、CSV3列目がTimeZoneであることを確認。CabrilloはWWROFとCQ WWの公開書式を参照しました。

QtでJARL段階5、Cabrillo段階5、テンプレートQSO列編集を描画して画像確認。800ピクセル幅でも左右配置とフォーム内スクロールが使えます。Windows EXEのビルド・Windows10/11実機動作、JARL/Cabrilloの提出受付、zLogの実機再読込は未検証です。新規テンプレートは編集開始用の構成で、公式大会プリセットとしては配布していません。

## 2026-09-11 クラブDB・利用者確認済み所在地

全145テスト成功。追加10テストで英字付きクラブ番号・先頭ゼロ、887件のマスター読込、メタデータ不一致の停止、100件ページ検索、R2の英字付き番号出力、R1の手入力保護、所在地保存の分離と再利用、外部変更停止と解除履歴、JCG町村の適用範囲、保存チェック/キャンセルの動作、HAMLOG取り込みでの再利用と補助OFFを確認しました。

クラブ検索と所在地の保存チェックをQt offscreenで描画し、800ピクセル幅の画像を確認。Windowsビルドには配布マスター3ファイルだけを指定し、利用者のlocation_overrides.json等を混入させない構成にしました。Windows EXE・実機の検証は未実施です。

## 2026-09-11 採点結果の全件閲覧
全147テスト成功（Linux Qt offscreen）。205件の最終ページ、101件目以降の未分類モード、絞り込み後の対象番号・原本行対応、未確認候補、再計算後とクリア後の古い表示除去を検査。最終ページを描画確認。Windows実機・EXEは未検証。

## 年変更編集と回復
全155テスト成功（Linux Qt offscreen）。追加8件と旧年変更拒否テストの変更はYEAR_MOVE.mdを参照。Windows実機と物理電源断は未検証。

## 一括処理の回復
全158テスト成功（Linux Qt offscreen）。新規3件と既存中断試験の拡張内容はBATCH_RECOVERY.mdを参照。Windows実機・EXEは未検証。

## 国名補助
全162テスト成功（Linux Qt offscreen）。追加4件はCOUNTRIES.mdを参照。Windows実機・EXE未検証。

## コンテスト地域参照
164テスト成功。追加2件で候補の明示選択、102件のページ送り、不明運用、手修正と地域/プリフィックス保持、大陸条件の採点と欠落/不正時停止を確認。Qt候補画面も描画確認。Windows実機・EXE未検証。

## 条件別マルチ抽出
全167テスト成功（Linux Qt offscreen）。追加3件と画面描画確認はMULTI_EXTRACTION.mdに記載。Windows実機未検証。

## ADIF国名補助
全170テスト成功（Linux Qt offscreen）。追加3件で空QTHの候補、GL保持、既存QTH優先、入力国名優先、曖昧移動局、インポート設定ON/OFFを確認。Windows実機・EXE未検証。

## パスとヘルプ
既存170テスト成功。追加の表示確認で、狭幅では中間省略、広幅では全文復帰、ツールチップに元文字列が保持されることを確認。ヘルプ画面をQt offscreenで描画確認。Windows実機でのフォルダー起動は未検証。

## ADIF所在地DB連携
全173テスト成功（Linux Qt offscreen）。追加3件でJCG町村/gun/元RMKS/GL保持、JCC種別不一致、未登録、明示国外との矛盾、JD1不確定、補助OFF、利用者確認済み表記の利用を確認。Windows実機・EXE未検証。

## 採点の分割処理
全176テスト成功（Linux Qt offscreen）。追加3件で1,250件の同期/分割結果一致、100件単位進捗、中止・再計算、計算中の外部ルール変更時の結果破棄を確認。Windows実機・EXE未検証。

## 入力エラー案内
178テスト成功（Linux Qt offscreen）。追加2件で実在しない日付・不正時刻・送受RST・区切り文字の対象欄識別、全角日時正規化、未知MODE自由入力、フォーカスと全選択を確認。Windows実機未検証。


2026-09-11: ADIF QSL出力更新
LoTW.R・eQSL/eQSL.Rをサービス別標準項目へ出力。RMKS原文保持、裸のLoTWは送信済みと推定しない。詳細ADIF_QSL_EXPORT.md。Linuxオフスクリーン180テスト成功。Windows EXE・実機未検証。標準QSL逆変換と紙カード等の標準項目マッピングは残課題。


2026-09-11: ADIF QSL取り込み更新
LoTW/eQSL標準項目の限定的な逆変換を実装。送信・受領が両方Yの場合に.R化、eQSL送信済みはeQSLへ。元項目保持、矛盾時の確認表示。詳細ADIF_QSL_IMPORT.md。上記の逆変換未実装との記載は本更新で部分的に解消。紙カード系・受領情報のみの自動判定等は対象外。Linuxオフスクリーン184テスト成功、Windows EXE・実機未検証。


2026-09-11: ADIF相手名取り込み更新
NAME/NAME_INTLをOP:へ反映、既存OP保持。STATION_CALLSIGN欠落時にOPERATORで自局照合。詳細ADIF_IMPORT_MAPPING.md末尾。188テスト成功、Windows実機未検証。


2026-09-11: ADIFバンド整合チェック
BANDとFREQを併記した入力でもFREQの数値・有限性・対応帯域を検査し、不一致は交信単位のエラーとして表示。BANDを黙って優先しない。正常な実周波数はADIF_EXTRAへ保持。判定帯域は既存のRANGESを使用し、帯域の追加や国内運用可否の判定を行う変更ではない。ADI/ADX、矛盾、不正数値、片方のみの入力を確認。全192テスト成功（Linux offscreen）。Windows EXE・実機未検証。残作業の最新情報はREMAINING_1.00.txt統合版を参照。


2026-09-11: ADIFモード対応更新
FT2等を追加。通常ADI/ADXとPOTAはADIF_VER 3.1.7。既知MODE/SUBMODE矛盾を検出、未知入力は保持。詳細ADIF_MODES.md。196テスト成功、Windows実機未検証。


紙カードADIF出力更新: BURO/CARD/my1wayの発送、BURO.R/CARD.R/1way.R/Direct.Rの受領を標準項目へ反映。受領だけでは発送済みとしない。従来の紙カード出力対象外という記載を本更新で置き換える。詳細ADIF_PAPER_QSL.md。200テスト成功、Windows実機未検証。


2026-09-12: ビューロ標準項目の取り込みを実装。経路Bかつ状態YをBURO/BURO.Rへ反映し、元項目保持。発送不明は返送要否の確認、曖昧な経路は推測変換しない。従来の紙カード逆変換未実装の記載を部分的に更新。詳細ADIF_BUREAU_IMPORT.md。204テスト成功、Windows実機未検証。


2026-09-12: HAMLOG出力Name/QSL対応
明確なOP:をNameへ、紙カード送受領を位置固定QSLへ反映。RMKS保持。再取り込み時のQSL空白保持、OP重複抑止。詳細HAMLOG_EXPORT_MAPPING.md。207テスト成功。HAMLOG・Windows実機未検証。従来のName/QSL出力空欄という説明を更新。


2026-09-12: コンテスト県コード補助を追加。詳細は CONTEST_AREA_HINTS.md。
Linux Qt offscreen 全210テスト成功。Windows実機未検証。残り大分類は4区切り。

2026-09-12: コンテスト大量件数対応。読込/出力準備/保存を背景処理化、採点表示を100件分に限定。
212テスト成功、合成1万交信の全件出力を検証。詳細: CONTEST_VOLUME.md。Windows未検証。

2026-09-12: コンテスト各形式の画面操作検証を追加、全214テスト成功。
CONTEST_WORKFLOW_CHECK.mdの対応範囲でコンテスト工程を完了。残り大分類3区切り。Windows実機未検証。
