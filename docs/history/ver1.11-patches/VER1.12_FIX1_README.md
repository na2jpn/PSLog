# PSLog Ver1.12 FIX1

適用元: PSLog Ver1.12（PATCH01/FIX適用済みの現在のWindows確認ソース）。

## 修正内容

1. 「フリラタブを追加」で入力エラーが出ても、追加ダイアログを閉じないように修正。
   - 自局コールサイン不正時は警告後に同じ画面へ戻ります。
   - 入力済みの種類・機種名などは保持します。
   - 問題のある入力欄へフォーカスを戻します。
2. カタカナのフリラコールは従来方針どおり、
   「PSLogでは平仮名を使います。OK」表示後に平仮名へ強制変換して続行します。
3. 機種名を任意項目へ変更。
   - 空欄のままフリラログブックを作成できます。
   - 機種名なしのファイル名は `YYYY_コール_種類.txt` とします。
   - 既存の `YYYY_コール_種類_機種名.txt` との互換を維持します。
   - 機種名なしと機種名ありのログは混在しても別ログブックとして扱います。
4. 「フリラタブを呼び出す」で機種名なしの場合は `（機種名なし）` と表示します。

## 変更ファイル

- `main.py`
- `free_radio.py`
- `test_free_radio_111.py`

## GPT側確認

- 変更Pythonファイル構文確認: OK
- フリラ関連テスト: 8件 OK
- 関連非GUI回帰テスト（フリラ/復元/セッション/ストレージ）: 39件 OK
- 全体discover: 502件実行、機能FAILなし。PySide6未導入環境のためGUI系69件はImportError、9件skip。
- Windows GUI確認はユーザー側で実施してください。
