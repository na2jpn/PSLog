# PSLog Ver1.061 PATCH01 FIX2

## 修正内容

FIX1で変更した `test_v101.py` の期待値だけを修正します。

`RulesDialog`（コンテストルール管理一覧）はVer1.06以前からルール名の末尾に内部ID（例: `[shiga]`）を表示しています。FIX1では由来接頭辞 `（ユーザー定義）` の確認を追加した際、誤って内部IDが表示されない前提の完全一致テストにしていました。

正しい期待値は次です。

`（ユーザー定義）滋賀コンテスト（2026） [shiga]`

コンテスト提出画面の選択リストは従来どおり内部IDを表示しないため、こちらは

`（ユーザー定義）滋賀コンテスト（2026）`

を期待します。

## アプリ本体

アプリ本体のコード変更はありません。PATCH01 + FIX1 適用後のソースへ本FIX2を上書きしてください。
