# Ver1.069 コンテストルール表示整備・途中成果

適用先は添付の `PSLog_1.069_WORK_HANDOFF_SOURCE(1).zip` のフルソース（PATCH09 + FIX1 + FIX3）です。ZIP内の相対パスをそのまま上書きします。VERSIONは1.069のままです。この差分は87大会整備の完了版ではなく、確認済みの5大会を反映した途中成果です。FIX番号を確定せず別名にしています。

- XPOの呼び出し方法を含め、XPO・秋田・福岡・QSOパーティ・島根の表示専用データを保存済み2026規約に照らして追加しました。
- QSOパーティのJARL公式URL、島根県支部の2026規約掲載ページを登録しました。秋田・福岡は公式PDFが保存されていますが、オンライン公式URLは未特定のため空欄のままです。
- `rule_view` 欠落大会の呼び出し方法に提出用文章を流用しなくなりました。未登録と表示します。
- 監査CSVは87大会全件の9項目、出典、残課題を記載します。81大会は9項目とも未登録です。
- 採点・提出判定・確認チェックボックス・OPPLACE条件は変更していません。

WindowsでのGUI表示とEXEビルドは未確認。`python -m unittest discover -v` はLinuxではPySide6を必要とする既存テストを含みます。`test_contest_rule_view_audit_87.py` と `test_patch09_fix1.py` を非GUI回帰として実行してください。
