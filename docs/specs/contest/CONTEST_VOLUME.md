# コンテスト大量件数対応 — PSLog 1.00

対象ログ読込・提出ファイル準備（再採点を含む）・保存を別スレッドで実行します。
Qt画面は待機表示を続け、作業中は他の入力を受け付けません。
途中でウィンドウだけ閉じて処理が残ることを防ぐため、待機画面にキャンセルはありません。
例外は従来の画面へ返し、外部変更確認と同名回避保存は従来の処理を使います。

採点結果の表示文字列とツールチップは表示中の100件だけ作成します。
未確定フィルター・元の交信番号・原本行の表示は保持します。
出力プレビューは先頭1500行、保存は全件です。

## 検証
Linux / Python 3.12 / Qt offscreen: 自動テスト212件成功。
check_contest_volume.pyで一時フォルダー内に合成10000交信を生成して検証。
読込0.241秒、採点・末尾ページ表示・zLog準備・保存まで合計0.584秒。
GUIタイマー51回動作。CSV10001行（ヘッダー込み）・34列を確認。
元ログ不変、保存直前に原本が外部変更された場合の停止を確認。
時間はこの環境での参考測定で、Windows上の速度保証ではありません。
この大量件数測定の出力形式はzLogです。JARL/Cabrilloの形式検証は既存テストに含まれます。

全データはメモリー上に保持します。無制限件数対応やネットワークドライブの応答性を保証しません。
所在地/国候補の作成・一括ナンバー補助など一部は同期処理です。
Windows実機・各形式を通した最終操作確認は残作業です。
