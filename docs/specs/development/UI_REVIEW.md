# 画面・説明書の整理確認 — PSLog 1.00

2026-09-12 / Linux Qt offscreen / 全218テスト成功。

今回の追加確認は、空のテスト用保存先から主要10画面を表示し、
480×320論理ピクセルの作業領域を模擬した操作到達性の確認です。

対象: インポート、エクスポート、個別ログ復元、検索編集、設定、
ブラックリスト管理、QSL受領一括、POTA、SOTA、使い方。

- ウィンドウ本体が指定作業領域以内に収まる。
- 外側スクロールが存在する。
- 設定などの各タブを切り替えられる。
- 内側/外側スクロールを使い、表示中の有効な操作ボタンへ到達できる。
- キャンセルで閉じ、ログTXTが作成されない。

インポート画面には内側のスクロールもあります。
今回の確認はQtによる表示位置・操作の自動検証で、Windows実画面の目視評価ではありません。
個別編集・削除やコンテストの処理内容は既存の各機能テストで確認しています。
全入力値・全エラー経路・全解像度の保証ではありません。

操作案内はUSER_GUIDE.txtとアプリ内ヘルプで共通本文を使用します。
READMEから最新の進捗・検証・項目対応を参照でき、古い進捗はdocs/historyへ分離しました。
保存先はSAVE_LOCATION.md、コンテストはCONTEST_WORKFLOW_CHECK.mdを参照してください。

画面・説明書の開発環境での整理工程を完了とします。
Windows上のDPI・モニター・長いパス・目視操作確認はWindows実機工程に残ります。
