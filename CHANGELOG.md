# Ver1.14 正本（2026-09-25）

- Ver1.13正本へ、利用者確認済みのVer1.14初回起動差分を統合して正本化。
- 初回起動で「アマチュア無線のコールサイン」と「フリラのコールサイン」を切替可能にし、フリラだけでPSLogを開始できるようにした。
- フリラ初回開始時に種類、機種名（任意）、年（自動）を設定し、初回の空標準タブをフリラタブへ置換。
- フリラ専用利用時にアマチュア用 `own` をフリラコールで上書きしないよう分離。
- フリラタブから標準/EASY/コンテストを追加した際にフリラコールをアマチュアコールとして引き継がないよう修正。
- 機種名空欄のフリラタブを再起動後も正常復元するよう修正。
- Ver1.14差分のREADME/manifestを `docs/history/ver1.14-diff/` へ履歴化。

# Ver1.13 正本（2026-09-25）

- Ver1.10正本へVer1.11 PATCH01、PATCH01 FIX1、Ver1.12 PATCH01 FIX2、Ver1.12 FIX1の受入済み状態を統合し、VERSIONを1.13へ繰り上げて正本化。
- `logbook_flr/` をフリラ原本として実運用化。フリラタブ最大4枚、既存ログブック呼び出し、重複タブ防止、水色選択色を追加。
- フリラコールの平仮名＋英数字ルール、カタカナ→平仮名変換、種類＋任意機種名＋年のログブック識別、機種名サジェストを追加。
- CB/LCR/DCR/UHFCB/特小/BLU/ETCを追加し、BAND/MODEを種類から自動設定。
- フリラLogSearchと専用検索・編集を追加。BAND/MODEの代わりに種類を表示し、同じ自局コールの全種類を横断。アマログとフリラログの検索経路は分離。
- 全体バックアップ/復元へ `logbook_flr/` を追加。
- Windows配布ZIP名の版数直書きを廃止し、`storage.VERSION` から自動生成。
- フリラタブ追加ダイアログの入力エラーで画面が閉じる問題を修正。機種名空欄を正式に許可。
- Ver1.11/1.12のPATCH/FIX README・manifestを `docs/history/ver1.11-patches/` へ整理。

# Ver1.10 正本（2026-09-24）

- Ver1.07正本へVer1.071〜1.078の受入済みPATCH/FIXを統合し、VERSIONを1.10へ繰り上げて正本化。
- QSL/JCC-JCG処理、JCG郡単位所在地、QSLショートカット、JCC/JCG交信チェック、CFM残数表示を統合。
- アマチュアログ取得を共通化し、将来フリラ用 `logbook_flr/` を予約・起動時生成・Updater保護対象化。
- EASYタブ最大2枚、専用2ページ入力、LogSearch常設、5分以上の時刻差確認、入力保持/クリア、緑枠を統合。
- 「PSLogについて」にPS=Pipe Separate（パイプセパレート）を明記。
- Ver1.071〜1.078のPATCH/FIX README・manifestを `docs/history/ver1.07-patches/` へ整理。
- Windows配布名を `PSLog_1.10_Windows_<timestamp>.zip` に更新。

# Ver1.078 PATCH08（2026-09-24）

- EASY入力1/2ページのバンド表記を `バンド` から `バンド（MHz）` へ変更し、入力値の単位を明確化。標準タブ・コンテスト画面の表記は変更しない。
- EASY入力グループボックスの外枠をPSLogの緑系アクセント `#6fa37d` に変更。EASY内の子グループボックスや他画面の枠線には影響させない。
- VERSIONを1.078へ更新。

# Ver1.077 PATCH07（2026-09-24）

- EASYタブを標準画面共用から専用2ページ入力画面へ変更。右側LogSearchは常時表示のまま、文字をやや大きくして視認性を向上。
- EASY 1/2ページはBand・Mode/Sub・相手コール・送受RSTに絞り、大きめの入力UIと「次へ」を配置。Band/Modeは前回値を継続。
- EASY 2/2ページは1ページ目の確認表示、His QTH（JCC/JCG→名称補助）、My QTH、RMKS（メモ）、編集可能なJST時刻、登録ボタンを配置。RMKS2とログ付与文字UIはEASYでは使用しない。
- EASY登録時、入力時刻と現時刻が5分以上ずれた場合は「キャンセル／現時刻で登録／入力された時刻で登録」を選択。5分未満でも登録確認ダイアログを表示。保存後は1ページ目へ戻り、Band・Mode/Sub・My QTHだけを保持。
- EASYタブ名はコールサイン本体のみを使う`[E]CALL`形式とし、標準ログの付与文字は表示・保存対象にしない。`/1`等はコールサインの一部としてそのまま保持。
- 「PSLogについて」をアイコン・見出し・説明カードを使うレイアウトへ更新し、PS=Pipe Separate（パイプセパレート）とPSログ形式、JARL電子ログ/Cabrillo等の出力対応を説明。
- VERSIONを1.077へ更新。

# Ver1.076 PATCH06（2026-09-24）

- `＋タブ`へ「EASYタブを追加」を追加。EASYタブは最大2枚とし、第1段階では標準タブと同じ交信画面・ログ保存処理を共用する。自局コール未設定時は`EASYタブ`、設定後は`[E]JH1HST`形式で表示し、セッション保存・復元・最近閉じたタブにも対応。
- JCG所在地の人間向けローマ字表記を、`Ina gun`ではなく`Inagun`のように郡名と`gun`を連結する形式へ統一。参照DB原本は変更せず、HIS QTH候補・コード反映時に正規化。
- 標準系のQSO保存後に前交信のHIS QTHとJCC/JCGを入力欄から消去し、MY QTHは保持するよう変更。
- VERSIONを1.076へ更新。

# Ver1.075 PATCH05（2026-09-24）

- JCC/JCG交信チェックの都道府県折りたたみ行を整理し、`全JCC-QSL済`/`全JCG-QSL済`時は包含関係にある`全JCC-交信済`/`全JCG-交信済`を重複表示しないよう変更。
- 同行末尾にCFM基準の未確認残数を`（CFM 残JCC N　残JCG N）`形式で表示。未交信も未CFMとして数え、残数0・該当種別なしは表示を省略。
- 通常ログとコンテストの交信入力で「相手コールサイン」ラベルとコールサイン入力文字を太字化し、入力時の視認性を向上。
- VERSIONを1.075へ更新。

# Ver1.074 PATCH04 FIX2（2026-09-24）

- Windows/PySide6でJCC/JCG交信チェックの都道府県親行描画時に `QTreeWidget.setFirstItemColumnSpanned` が存在せず、集計後の表示で `AttributeError` になる問題を修正。
- Qt6/PySide6の `QTreeWidgetItem.setFirstColumnSpanned(True)` を使用し、都道府県親行の全列スパン表示を維持。
- 集計ロジック、ワーカースレッド、Loading表示、遅延子行生成の仕様は変更なし。
- VERSIONは1.074のまま。

# Ver1.074 PATCH04 FIX1（2026-09-24）

- JCC/JCG交信チェックの集計をGUIスレッドからワーカースレッドへ移し、集計中もウィンドウが応答するよう修正。
- 集計中は「集計しています…」と不定進捗のLoading表示を出し、対象コール・サフィックス条件・集計/閉じる操作を一時無効化。
- 集計結果の初期描画は47都道府県の親行だけに限定し、各都道府県のJCC/JCG詳細行は展開時に初回生成する遅延表示へ変更。約1,000地域×多数バンドのセルを集計直後に一括生成する負荷を解消。
- VERSIONは1.074のまま。

# Ver1.074 PATCH04（2026-09-24）

- 交信編集のQSL追加へ`QRZ.R`と`CARD.R`を追加し、ログ検索・編集にQSL専用のコンパクト処理画面を新設。既存QSL正規化・原本保護処理を共通利用。
- 「提出ログファイル作成 > アワード」にJCC/JCG交信チェックを追加。対象自局をログから選び、明示的な「集計」操作で都道府県別・全バンド/各バンド別の交信/QSL状況を折りたたみ表示。JCG末尾英字は無視して郡単位で集計。
- 都道府県単位で`全JCC-交信済`、`全JCC-QSL済`、`全JCG-交信済`、`全JCG-QSL済`を判定。現行所在地DBを優先し、政令指定都市区は親市、東京23特別区は現行JCC相当単位として扱う。
- アマチュアログ取得を`logbook_sources.py`へ共通化し、将来フリラ用`logbook_flr/`を予約。起動時に`logbook/`と`logbook_flr/`を生成し、更新パッケージでも`logbook_flr/`を利用者データとして保護。
- VERSIONを1.074へ更新。

# Ver1.073 PATCH03（2026-09-24）

- JCGの5桁コードだけが記録されている場合、所在地DBの該当町村から共通する郡（gun）を安全に確定できれば、特定の町村を推測せず郡単位のHIS QTHへ反映するよう変更。町村識別英字付きJCGは従来どおり詳細所在地へ反映。
- 交信編集の「HIS QTHへ反映」だけでなく、JCC/JCG一括処理[A]とRMKS→JCC/JCG+HIS QTH[B]にも同じ郡単位解決を適用。東京島嶼部など、現行DBから郡・支庁単位を安全に確定できない例外は推測しない。
- PATCH01でメールアドレスをJARL提出の必須項目にした後も、旧ワークフロー回帰テストがメール未入力のままだったため、テスト入力を現仕様へ更新。報告された4件のFAILが本来確認したい後段処理へ進めるよう修正。
- 所在地DB仕様へ「JCGは郡までを有効な所在地情報として扱い、町村名は追加情報」という方針を明記。
- VERSIONを1.073へ更新。

# Ver1.072 PATCH02（2026-09-24）

- JCC/JCGログ一括処理のRMKS参照[B][C]で、RMKS1中のPSLog正式QSLタグ（`.R`あり/なし）と、小数点を含む単独数値の周波数メモを無視してコード判定できるよう拡張。RMKS2は従来どおり参照しない。任意の文章や `7MHz` 等から数字だけを拾う推測は行わない。
- QSL受領一括TXTを従来の「日付・時刻・相手コール」3項目に加え、任意JCC/JCGを加えた4項目へ対応。3/4項目とも順序自由で、時刻のコロン必須は維持。海外局等は3項目のままでよい。
- QSL受領画面のTXT形式説明を4項目対応へ更新。任意JCC/JCGは確認情報として表示し、QSL受領処理自体は従来どおりRMKSのみを更新してログのJCC/JCG欄は変更しない。
- VERSIONを1.072へ更新。

# Ver1.071 PATCH01（2026-09-24）

- コンテスト提出のJARL R1.0/R2.1最終入力でメールアドレスをPSLog標準の必須項目に変更。大会個別のrequired_fields指定がなくても「入力内容を確認」で空欄を拒否し、画面上も赤い＊を表示。低レベル読込互換は維持。
- 愛・地球博記念コンテストの提出時刻初期値がJSTであることを回帰テストへ明示。既存ルールの`submission.zone = JST`と出力時のJST限定動作は変更なし。
- 「2 対象ログ」の対象自局フォームを垂直Fixedへ変更し、ログ一覧までの不要な空白を抑制。
- 「3 交信ナンバー確認」の「↓↓↓」を固定高にし、上下の余白を圧縮。
- コンテスト概要の送信出力は、表示専用`event.rule_view.power`がある場合それを優先。愛・地球博では「電話部門は20W以下（HF帯は10W以下）」を表示し、QRP等の個別種目値を共通上限のように混在表示しない。
- VERSIONを1.071へ更新。

# Ver1.07 正本（2026-09-24）

- Ver1.069コンテストルール整備完了ソースへVer1.070 PATCH10 + FIX2を統合し、Ver1.07正本化。
- コンテスト管理・編集一覧へ開催年月表示を追加。
- 提出最終設定へ標準のバンド順並べ替えを追加し、`event.submission.order=band_time` で初期ONを指定可能。愛・地球博記念コンテストは初期ON。
- 同梱ルール出自判定をソース実行/frozen配布実行で整理し、同名ファイルだけによる誤った `user_modified` 判定を防止。
- 利用者Windows環境でVer1.070 + FIX2のビルド・起動成功を確認。正本化時は機能ロジックを増やさずVERSIONと配布名を1.07へ確定。
- 旧Ver1.06開発ラインのPATCH/FIX/Work文書を `docs/history/ver1.06-patches/` へ整理。

# Ver1.070 PATCH10（2026-09-23）

- コンテストルール管理・編集の年別一覧と前年比較一覧に、開催開始月を `（2026/9）` の形式で表示。ほかのコンテスト選択画面の年表示は変更しない。
- JARL提出情報に標準オプション「ログをバンド順に並べる（同一バンド内は時刻順）」を追加。大会を問わず提出時にON/OFFできる。
- `event.submission.order=band_time` の大会は上記オプションを初期ONとし、愛・地球博記念コンテストは既存ルール定義により初期ON。ユーザーがOFFへ変更した場合は時系列/選択順を維持する。
- VERSIONを1.070へ更新。

# Ver1.069 コンテストルール表示・先行15大会パッチ（2026-09-23）

- 現行Ver1.069 PATCH09 + FIX1 + FIX3の引継ぎソースを基準に、先行15大会の表示専用 `rule_view` を公式規約と確認済み指示に合わせて整備。
- 周波数制限を交信ルール表示から除き、「特殊なルール」に個別条件を整理。未登録の送信出力は「制限なし」と表示し、未登録の移動運用行は非表示。
- 得点・マルチ・締切などの表示用文章を提出・採点データから分離。ALL JA4の公式規約URLを更新。
- QSOパーティをコンテスト一覧から除外。専用QSOパーティ表示と既存ルールは維持。
- 15大会の生成元を追加し、87ファイル読込・表示と提出／採点データ非変更の回帰テストを追加。VERSIONは1.069のまま。

# Ver1.069 PATCH09

- ファイルメニューを整理し、本体/ログ/出力/レポートの各保存場所を開く項目、PSLog再起動、PSLog終了を明確な順序と区切り線で配置。
- 交信編集画面のRMKS直下に `[LoTW.R]` `[hQSL.R]` `[eQSL.R]` `[BURO.R]` `[Other.R]` のQSL追加ショートカットを追加。
- QSL追加は既存QSL正規化処理を利用し、元タグから `.R` への昇格と重複防止を行う。
- 交信編集のRSTs/RSTrを1段横並びへ変更し、縦方向の増加を抑制。
- VERSIONを1.069へ更新。

# Ver1.068 PATCH08 FIX1

- QSL一括受領の複数候補を排他的ラジオボタンからチェックボックスへ変更し、1つの受領TXT行から複数QSOを明示選択できるよう修正。
- 入力時刻±3分にあるコール違いQSOを「手動候補」として処理詳細画面へ展開し、移動表記違い・プリフィックス違い等を人間判断で受領対象へ割り当て可能にした。
- 手動候補がない遠隔の未一致（例: 時刻差361分）は従来どおり未処理として残す。
- 同一ログQSOを別入力行から重複選択した場合の既存重複防止を維持。
- 複数QSO選択に合わせ、結果CSV・結果TXT・未処理TXTの判定を入力行単位で正しく集計するよう調整。
- VERSIONは1.068のまま。

# Ver1.068 PATCH08

- QSL一括受領「処理の詳細」の複数候補を、同一入力行ごとの排他的ラジオボタンで選択できるよう変更。
- 一意一致はチェックボックス、複数候補はラジオボタンとし、選択意図をUI上で明確化。
- 複数候補のラジオ選択は候補行・詳細表示と連動し、「全て解除」で未選択へ戻せる。
- 先頭列名を「処理」から「選択」へ変更。
- VERSIONを1.068へ更新。

# Ver1.067 PATCH07

- QSL一括受領の照合詳細を色付きブロック表示へ変更。未一致・複数候補等の入力/照合状態/理由は赤字、同一コール候補は薄黄色、入力時刻±3分のコール違いQSOは薄ピンクで表示する。
- QSL処理詳細画面の実行ボタンを `チェック済を受領［<受領方法>］にする` へ変更し、実行対象の受領方法を明示する。
- QSL処理結果TXTの末尾に `===== QSL対象となったPSLog原本行 =====` セクションを追加し、今回実際に更新されたQSOの更新後PSLog原本行をそのまま列挙する。
- VERSIONを1.067へ更新。

# Ver1.066 PATCH06 FIX1

- QSL一括受領の周辺QSO抽出で、同一日時に複数QSOが存在する場合に `Hit` 同士の比較が発生して `TypeError` になる問題を修正。
- 周辺QSO一覧のソートを日時キー明示に変更。
- 同一日時複数QSOの回帰テストを追加。
## Ver1.069 PATCH09 FIX1 (2026-09-22)
- コンテスト規約表示に表示専用 `event.rule_view` を追加し、採点・提出制限と分離。
- 愛・地球博記念コンテスト2026の呼び出し、交信対象、周波数、移動、出力、D-STAR等を公式規約に合わせて修正。
- 愛・地球博の根拠未確認の一般 `required_flags`（レピータ/クロスバンド/リモート/セルフスポット等）を削除。
- 呼び出し方法/周波数制限を無関係な確認文から誤推測しないよう修正。
- ルール表示に「交信上の禁止・制限」を追加。
- `devtools/audit_rule_view.py` を追加。

## Ver1.069 PATCH09 FIX3

- Windows/PySide6で `test_file_menu_order_and_separators` が
  `Internal C++ object (QMenu) already deleted` になる問題を根本回避。
- ファイルメニュー項目順を `FILE_MENU_ITEMS` に一元化。
- 実際のGUIメニュー生成も `FILE_MENU_ITEMS` を使用。
- 回帰テストも同じ `FILE_MENU_ITEMS` を検査し、QMenuラッパーの寿命管理に依存しない方式へ変更。
- ファイルメニューの実際の表示内容・順序は変更なし。
- VERSIONは1.069のまま。

# Ver1.075 PATCH05 FIX1（2026-09-24）

- PATCH05で追加したGUI回帰テストが、実在しない `main.Main` を参照してWindows/PySide6環境でImportErrorになる問題を修正。
- 正しいメインウィンドウクラス `main.Window` を用いて、通常ログ/コンテスト双方の相手コールサインラベル・入力欄の太字を検証するよう修正。
- PSLog本体機能およびVERSIONは変更なし（1.075）。

# Ver1.075 PATCH05 FIX2（2026-09-24）

- PATCH05 GUI回帰テストが空の一時データルートで `main.Window` を生成した際、実運用向けの初回起動 `QInputDialog` が0msタイマーで起動し、offscreenテスト環境では見えないモーダル待ちになってテストが停止する問題を修正。
- GUIテスト開始前に `save_settings(..., {'own':'JH1HST'})` で自局コールを用意し、初回起動ダイアログを発生させずに太字UIだけを検証するよう変更。
- PSLog本体機能およびVERSIONは変更なし（1.075）。
