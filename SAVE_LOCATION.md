# 保存場所と起動時の案内 — PSLog 1.14

通常のデータ基準位置は、ソース実行時はソース所在フォルダー、EXE版はEXE所在フォルダーです。ショートカットの作業フォルダーを変更しても基準は変わりません。開発テスト用に明示した `data_root` は指定を優先します。

## 主なruntimeフォルダー

- `logbook/` — アマチュア無線のPSLogマスターログ。
- `logbook_flr/` — フリラ専用のPSLogマスターログ。
- `bak/` — 日常バックアップ。
- `bak/logbook_bak/` — ログファイル統合の実行前バックアップ。
- `output/contest/` — コンテスト提出ファイル。
- `output/export/` — 通常エクスポート。
- `output/pota/` — POTA出力。
- `output/sota/` — SOTA出力。

起動時に `logbook/` と `logbook_flr/` が無ければ生成します。アマチュア無線の検索・集計は共通のアマチュアログ取得経路を使い、`logbook_flr/` を混在させません。Updaterは `config/`, `logbook/`, `logbook_flr/`, `bak/`, `output/` を利用者データとして保護します。

各出力画面では標準保存先から任意の別フォルダーへ変更できます。

起動時に設定読込等が失敗した場合は場所と理由を表示して終了し、別フォルダーへの黙った切替や設定/ログの自動初期化は行いません。

配布時は書き込み可能な専用フォルダーへ一式を置く運用を想定します。利用者ログや設定はEXEと一緒に移動する必要があります。
