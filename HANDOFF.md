# PSLog Ver1.14 正本 引継ぎ

## 正本と開発方針

`PSLog_1.14_CANONICAL_SOURCE.zip` が唯一の基準ソースです。Ver1.13正本へ `PSLog_1.14_DIFF.zip` を再適用しないでください。次の通常差分番号は原則 **Ver1.141** とします。次の正本番号は利用者指示を優先します。

要望を集めている段階では実装しません。利用者が「実装」等を明示してから、その時点までの要望をまとめて反映します。「まとめ」は整理・確認のみで実装指示ではありません。受け渡しは変更ファイルのみの差分ZIPを基本とします。

## データ互換の基礎

- PSLog TXTは11固定項目を維持。新規TXTはUTF-8 BOM + CRLF、既存はBOM有無双方を扱う。
- ログ日時はJST。RMKS2はRMKS内の予約区切りで保持し、12項目目を作らない。
- 原本保護、外部変更検知、旧バックアップ読込互換を壊さない。
- アマチュアログは `logbook/`、フリラログは `logbook_flr/`。検索・集計・編集の取得経路を混ぜない。
- `logbook/` と `logbook_flr/` は起動時に不足していれば生成。Updaterは双方をユーザーデータとして保護する。
- 全体バックアップ/復元はアマチュアログとフリラログの双方を対象にする。

## 初回起動 — Ver1.14追加仕様

- 初回起動ダイアログの初期表示は **「アマチュア無線のコールサイン」**。
- その下の **「アマチュア無線ではなくフリラのコールサイン」** ボタンでフリラ開始へ切り替えられる。逆方向にも戻せる。
- アマチュア開始時は従来の自局コール設定を使用する。
- フリラ開始時は、自局フリラコール、種類、機種名（任意）、年（自動）を設定する。
- フリラで確定した場合、初回の空標準タブをフリラタブへ置き換え、アマチュア無線コール未設定のまま開始できる。
- フリラ専用利用時はアマチュア設定 `own` をフリラコールで上書きしない。
- フリラタブから標準/EASY/コンテストタブを追加する場合、フリラコールをアマチュア自局コールの初期値として引き継がない。
- 機種名空欄のフリラタブも再起動後に正常復元する。

## フリラ — 継承仕様

### タブとログブック

- `＋タブ → フリラタブを追加…` で新しいフリラログブック/タブを作成する。
- フリラログブックの作成数自体に上限はない。同時に開けるフリラタブは最大4枚。
- タブ名は `[F]CALL 種類`。フリラタブの選択色は水色系。
- 同一 `自局コール + 種類 + 機種名` のログブックは重複タブを開かず、既存タブへ移動する。
- メニュー `フリラ` はヘルプ左に置き、`フリラタブを呼び出す…` と `フリラログ検索・編集` を持つ。

### 自局コール・ログ識別

- フリラコールは平仮名＋ASCII英数字のみ。ASCII英字は大文字化する。
- 全角/半角カタカナはNFKC正規化後に平仮名へ変換し、「PSLogでは平仮名を使います。OK」と案内する。
- 漢字など許可外文字は警告し、作成ダイアログを閉じずに入力欄へ戻す。
- ログブック識別は `種類 + 機種名 + 年`。機種名は任意。空欄を許可する。
- ファイル名は機種名あり `YYYY_CALL_種類_機種名.txt`、機種名なし `YYYY_CALL_種類.txt`。
- 機種名入力は自由入力＋既存 `logbook_flr/` から種類別サジェスト。

### 種類とBAND/MODE

| 種類 | BAND | MODE |
|---|---|---|
| CB | `27` | `AM` |
| LCR | `142/146` | `DIGITAL` |
| DCR | `351` | `DIGITAL` |
| UHFCB | `422` | `FM` |
| 特小 | `422` | `FM` |
| BLU | `2400` | `DIGITAL` |
| ETC | `N/A` | `N/A` |

BAND/MODEはPSLog 11項目との互換維持用に内部保存するが、フリラ利用者には種類から自動設定する。RST初期値は59。DATE/TIME、HIS QTH、MY QTH、RMKS、JCC/JCGは既存PSLog項目を利用する。

### LogSearch / 検索・編集

- フリラタブ右側LogSearchは、現在の種類だけでなく**同じ自局フリラコールの全種類**を横断する。
- フリラ表示ではBAND/MODEを出さず、代わりに `種類` を表示する。HIS CALLSIGNは先頭。
- メニューのフリラ検索・編集も `logbook_flr/` のみを対象とし、種類 `すべて` でCB/LCR/DCR等を横断できる。
- アマチュア検索・編集は `logbook/` のみ。フリラ検索・編集は `logbook_flr/` のみ。
- 同系統タブから検索を開く場合はその自局コールを初期選択。別系統タブから開く場合は `選択してください`。
- `選択してください` のまま検索は実行しない。
- フリラ編集ではBAND/MODEを手編集せず、種類から自動値を再設定する。

## コンテストルール表示

登録87ルールの内訳はコンテスト86件 + QSOパーティ1件。QSOパーティは専用機能を使用し、コンテスト一覧には出しません。

ルール表示は**コンテスト中に基本情報をすぐ確認する人間向け機能**です。表示を詳しくしたことを理由に、採点エンジン、提出必須欄、確認チェック、提出不可条件を勝手に増やさないでください。

## Ver1.14正本化

Ver1.13正本へ、利用者がWindows側で正常確認したVer1.14初回起動差分を統合して正本化しています。差分README/manifestは `docs/history/ver1.14-diff/` に履歴として保存し、今後の再適用対象にはしません。正本化時に受入済みVer1.14差分以外のランタイム機能変更は追加していません。
